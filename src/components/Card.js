function Card(props) {
//   const value = props.value;

  return (
    <div className="bg-yellow-200 h-[400px]">
      <p>
        img elements must have an alt prop, either with meaningful text, or an
        empty string for dec
      </p>
      <div>
        {/* <img src={value.images}></img> */}
      </div>
    </div>
  );
}

export default Card;
